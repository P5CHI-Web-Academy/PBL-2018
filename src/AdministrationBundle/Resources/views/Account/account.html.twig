{% extends '@Administration/base.html.twig' %}

{% block title %}Account{% endblock %}

{% block body %}
    <div class="body main">
        <div class="title-create-slide">
            <h3 class="title-create-slide-text">My Account</h3>
            <ion-icon name="person" class="icon-create-title"></ion-icon>
        </div>

        <div class="line-under-title"></div>
        {#<div class="icon-back-div">#}
            {#<a href="{{ path('locations') }}"><ion-icon name="arrow-round-back"></ion-icon></a>#}
        {#</div>#}

            username: {{ logged_in_user.username }}
        <br/>
            <a href="{{ path('change_password') }}">change password</a>
        <br/>
        <a href="{{ path('user_edit', {'id': logged_in_user.id}) }}">Edit account</a>
        <br/><br/>

        Slides:<br/>
        {% for slide in own_slides %}
            {{ slide.name }}

            <a href="{{ path('slide_preview', {'id': slide.id}) }}">Preview</a>
            <a href="{{ path('slide_edit', {'id': slide.id}) }}">Edit</a>
            <a href="{{ path('slide_delete', {'id': slide.id}) }}">Delete</a>

            <br/>

        {% endfor %}

        <br/>
        <a href="{{ path('slide_new') }}">Create new slide</a>
    </div>

{% endblock %}
